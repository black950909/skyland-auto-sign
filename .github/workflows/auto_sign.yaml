name: Auto Sign

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    steps:
      - uses: actions/checkout@v2

      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 如果沒有 requirements.txt 就直接裝 requests
          pip install requests

      - name: Run skyland.py
        id: skyland_run
        env:
          GitHub_Actions: true
          TOKEN: ${{ secrets.TOKEN }}
        # 使用 bash 的特有寫法確保日誌被正確寫入
        run: python skyland.py > output.log 2>&1 || true 

      - name: Discord Notification
        if: always()
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          # 檢查 Webhook 是否存在
          if [ -z "$WEBHOOK_URL" ]; then
            echo "錯誤：找不到 DISCORD_WEBHOOK Secret"
            exit 1
          fi
          
          # 擷取日誌內容
          RESULT=$(tail -n 15 output.log)
          
          # 發送通知
          curl -H "Content-Type: application/json" \
               -d "{\"content\": \"🚀 **明日方舟終末地簽到回報**\n\`\`\`\n$RESULT\n\`\`\`\"}" \
               "$WEBHOOK_URL"
